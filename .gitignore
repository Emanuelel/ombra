node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# tsc -b build output for the tsconfig.node.json project reference (vite.config.ts source
# of truth lives at the repo root, right next to its own emitted output — easy to recommit
# by accident, hence the explicit ignore).
/vite.config.js
/vite.config.d.ts
.env
.env.*
!.env.example
.vercel
.DS_Store

# build-only data artifacts (fetched/generated by scripts, not shipped)
src/data/buildings-all.json
src/data/poi-fsq.json
src/data/poi-overture.json

# local screenshot harness output
harness-shots/

# local Claude Code settings (may contain personal API tokens)
.claude/*.local.json
